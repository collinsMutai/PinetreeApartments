<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>Tenants</h1>
    <% if(tenants.length > 0) { %>
    <div>
        <% for (let tenant of tenants) { %>
        <p><%= tenant.name %> - <a href="/tenants/<%= tenant._id %>">Invoice</a></p>
        <% } %>
    </div>
    <% } %>
</body>
</html>